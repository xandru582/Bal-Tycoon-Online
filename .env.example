# ── Copia este archivo a .env y rellena los valores ───────────────
# cp .env.example .env

# Genera secrets seguros con: openssl rand -hex 32
POSTGRES_PASSWORD=cambia_esto_por_una_password_segura
JWT_SECRET=genera_un_secret_de_32_bytes_aqui
JWT_REFRESH_SECRET=genera_otro_secret_diferente_aqui

# Opcional: sobreescribir valores por defecto
# PORT=3001
# NODE_ENV=production
